import styled from 'styled-components'

export const DigitalWatchWrapper = styled.div`
    width: 15rem;
    height: 5rem;
    margin-top: 2rem;
    border-radius: 0.6rem;
    border: 0.063rem solid var(--gray-700);
    background-color: var(--gray-800);

    display: flex;
    align-items: center;
    justify-content: center;

    font-size: 2rem;
    font-weight: 700;
    color: var(--white);
`
